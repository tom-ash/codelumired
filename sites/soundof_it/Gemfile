source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in soundof_it.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end
